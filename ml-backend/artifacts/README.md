# This directory stores trained model artifacts (.joblib files).
# Files here are generated by the training pipeline and loaded by the FastAPI server.
# Do NOT delete this directory — it is required at runtime.
